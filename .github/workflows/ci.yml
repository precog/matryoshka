# This file was automatically generated by sbt-github-actions using the
# githubWorkflowGenerate task. You should add and commit this file to
# your git repository. It goes without saying that you shouldn't edit
# this file by hand! Instead, if you wish to make changes, you should
# change your sbt build configuration to revise the workflow description
# to meet your needs, then regenerate this file.

name: Continuous Integration

on:
  pull_request:
    branches: [master, backport/v*]
  push:
    branches: [master, backport/v*]

env:
  SBT: ./sbt
  REPO_SLUG: ${{ github.repository }}
  ENCRYPTION_PASSWORD: ${{ secrets.ENCRYPTION_PASSWORD }}
  GITHUB_ACTOR: precog-bot
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  build:
    name: Build and Test
    strategy:
      matrix:
        os: [ubuntu-latest]
        scala: [2.13.1, 2.12.10]
        java: [adopt@1.8]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout current branch (fast)
        uses: actions/checkout@v2

      - name: Setup Java and Scala
        uses: olafurpg/setup-scala@v5
        with:
          java-version: ${{ matrix.java }}

      - name: Cache ivy2
        uses: actions/cache@v1
        with:
          path: ~/.ivy2/cache
          key: ${{ runner.os }}-sbt-ivy-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}-${{ hashFiles('.versions.json') }}

      - name: Cache coursier (generic)
        uses: actions/cache@v1
        with:
          path: ~/.coursier/cache/v1
          key: ${{ runner.os }}-generic-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}-${{ hashFiles('.versions.json') }}

      - name: Cache coursier (linux)
        if: contains(runner.os, 'linux')
        uses: actions/cache@v1
        with:
          path: ~/.cache/coursier/v1
          key: ${{ runner.os }}-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}-${{ hashFiles('.versions.json') }}

      - name: Cache coursier (macOS)
        if: contains(runner.os, 'macos')
        uses: actions/cache@v1
        with:
          path: ~/Library/Caches/Coursier/v1
          key: ${{ runner.os }}-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}-${{ hashFiles('.versions.json') }}

      - name: Cache coursier (windows)
        if: contains(runner.os, 'windows')
        uses: actions/cache@v1
        with:
          path: ~/AppData/Local/Coursier/Cache/v1
          key: ${{ runner.os }}-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}-${{ hashFiles('.versions.json') }}

      - name: Cache sbt
        uses: actions/cache@v1
        with:
          path: ~/.sbt
          key: ${{ runner.os }}-sbt-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}-${{ hashFiles('.versions.json') }}

      - name: Check that workflows are up to date
        run: $SBT ++${{ matrix.scala }} githubWorkflowCheck

      - run: $SBT ++${{ matrix.scala }} ci

      - name: Upload target directory 'target' (${{ matrix.scala }})
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}-target
          path: target

      - name: Upload target directory 'docs/target' (${{ matrix.scala }})
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}-docs_target
          path: docs/target

      - name: Upload target directory 'repl/jvm/target' (${{ matrix.scala }})
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}-repl_jvm_target
          path: repl/jvm/target

      - name: Upload target directory 'core/js/target' (${{ matrix.scala }})
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}-core_js_target
          path: core/js/target

      - name: Upload target directory 'core/jvm/target' (${{ matrix.scala }})
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}-core_jvm_target
          path: core/jvm/target

      - name: Upload target directory 'tests/js/target' (${{ matrix.scala }})
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}-tests_js_target
          path: tests/js/target

      - name: Upload target directory 'repl/js/target' (${{ matrix.scala }})
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}-repl_js_target
          path: repl/js/target

      - name: Upload target directory 'tests/jvm/target' (${{ matrix.scala }})
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}-tests_jvm_target
          path: tests/jvm/target

      - name: Upload target directory 'scalacheck/jvm/target' (${{ matrix.scala }})
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}-scalacheck_jvm_target
          path: scalacheck/jvm/target

      - name: Upload target directory 'scalacheck/js/target' (${{ matrix.scala }})
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.scala }}-${{ matrix.java }}-scalacheck_js_target
          path: scalacheck/js/target

      - name: Upload target directory 'project/target'
        uses: actions/upload-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-project_target
          path: project/target

  publish:
    name: Publish Artifacts
    needs: [build]
    if: github.event_name != 'pull_request' && (github.ref == 'refs/heads/master' || startsWith(github.ref, 'refs/heads/backport/v'))
    strategy:
      matrix:
        os: [ubuntu-latest]
        scala: [2.13.1]
        java: [adopt@1.8]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout current branch (fast)
        uses: actions/checkout@v2

      - name: Setup Java and Scala
        uses: olafurpg/setup-scala@v5
        with:
          java-version: ${{ matrix.java }}

      - name: Cache ivy2
        uses: actions/cache@v1
        with:
          path: ~/.ivy2/cache
          key: ${{ runner.os }}-sbt-ivy-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}-${{ hashFiles('.versions.json') }}

      - name: Cache coursier (generic)
        uses: actions/cache@v1
        with:
          path: ~/.coursier/cache/v1
          key: ${{ runner.os }}-generic-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}-${{ hashFiles('.versions.json') }}

      - name: Cache coursier (linux)
        if: contains(runner.os, 'linux')
        uses: actions/cache@v1
        with:
          path: ~/.cache/coursier/v1
          key: ${{ runner.os }}-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}-${{ hashFiles('.versions.json') }}

      - name: Cache coursier (macOS)
        if: contains(runner.os, 'macos')
        uses: actions/cache@v1
        with:
          path: ~/Library/Caches/Coursier/v1
          key: ${{ runner.os }}-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}-${{ hashFiles('.versions.json') }}

      - name: Cache coursier (windows)
        if: contains(runner.os, 'windows')
        uses: actions/cache@v1
        with:
          path: ~/AppData/Local/Coursier/Cache/v1
          key: ${{ runner.os }}-sbt-coursier-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}-${{ hashFiles('.versions.json') }}

      - name: Cache sbt
        uses: actions/cache@v1
        with:
          path: ~/.sbt
          key: ${{ runner.os }}-sbt-cache-${{ hashFiles('**/*.sbt') }}-${{ hashFiles('project/build.properties') }}-${{ hashFiles('.versions.json') }}

      - name: Download target directory 'target' (2.13.1)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.13.1-${{ matrix.java }}-target

      - name: Download target directory 'target' (2.12.10)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.10-${{ matrix.java }}-target

      - name: Download target directory 'docs/target' (2.13.1)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.13.1-${{ matrix.java }}-docs_target

      - name: Download target directory 'docs/target' (2.12.10)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.10-${{ matrix.java }}-docs_target

      - name: Download target directory 'repl/jvm/target' (2.13.1)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.13.1-${{ matrix.java }}-repl_jvm_target

      - name: Download target directory 'repl/jvm/target' (2.12.10)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.10-${{ matrix.java }}-repl_jvm_target

      - name: Download target directory 'core/js/target' (2.13.1)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.13.1-${{ matrix.java }}-core_js_target

      - name: Download target directory 'core/js/target' (2.12.10)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.10-${{ matrix.java }}-core_js_target

      - name: Download target directory 'core/jvm/target' (2.13.1)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.13.1-${{ matrix.java }}-core_jvm_target

      - name: Download target directory 'core/jvm/target' (2.12.10)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.10-${{ matrix.java }}-core_jvm_target

      - name: Download target directory 'tests/js/target' (2.13.1)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.13.1-${{ matrix.java }}-tests_js_target

      - name: Download target directory 'tests/js/target' (2.12.10)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.10-${{ matrix.java }}-tests_js_target

      - name: Download target directory 'repl/js/target' (2.13.1)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.13.1-${{ matrix.java }}-repl_js_target

      - name: Download target directory 'repl/js/target' (2.12.10)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.10-${{ matrix.java }}-repl_js_target

      - name: Download target directory 'tests/jvm/target' (2.13.1)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.13.1-${{ matrix.java }}-tests_jvm_target

      - name: Download target directory 'tests/jvm/target' (2.12.10)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.10-${{ matrix.java }}-tests_jvm_target

      - name: Download target directory 'scalacheck/jvm/target' (2.13.1)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.13.1-${{ matrix.java }}-scalacheck_jvm_target

      - name: Download target directory 'scalacheck/jvm/target' (2.12.10)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.10-${{ matrix.java }}-scalacheck_jvm_target

      - name: Download target directory 'scalacheck/js/target' (2.13.1)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.13.1-${{ matrix.java }}-scalacheck_js_target

      - name: Download target directory 'scalacheck/js/target' (2.12.10)
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-2.12.10-${{ matrix.java }}-scalacheck_js_target

      - name: Download target directory 'project/target'
        uses: actions/download-artifact@v1
        with:
          name: target-${{ matrix.os }}-${{ matrix.java }}-project_target

      - name: Install Ruby
        uses: actions/setup-ruby@v1
        with:
          ruby-version: 2.6

      - name: Common sbt setup
        run: $SBT ++${{ matrix.scala }} transferCommonResources transferPublishAndTagResources exportSecretsForActions

      - run: ./scripts/commonSetup

      - name: Publish artifacts and create tag
        run: ./scripts/publishAndTag ${{ github.repository }}

  auto-merge:
    name: Auto Merge
    needs: [build]
    if: github.event_name == 'pull_request' && contains(github.head_ref, 'version-bump')
    strategy:
      matrix:
        os: [ubuntu-latest]
        scala: [2.13.1]
        java: [adopt@1.8]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout current branch (fast)
        uses: actions/checkout@v2

      - name: Setup Java and Scala
        uses: olafurpg/setup-scala@v5
        with:
          java-version: ${{ matrix.java }}

      - name: Install Ruby
        uses: actions/setup-ruby@v1
        with:
          ruby-version: 2.6

      - name: Common sbt setup
        run: $SBT ++${{ matrix.scala }} transferCommonResources exportSecretsForActions

      - run: ./scripts/commonSetup

      - name: Export CLONE_DIR
        run: 'echo ::set-env name=CLONE_DIR::$(mktemp -d /tmp/precog-bump.XXXXXXXX)'

      - name: Export PR_NUMBER
        run: 'echo ::set-env name=PR_NUMBER::$(echo $GITHUB_REF | cut -d''/'' -f3)'

      - run: ./scripts/checkAndAutoMerge