{
    "name": "flatten array",

    "backends": {"mongolabs": "verify"},

    "data": "zips.data",

    "query": "SELECT loc[*] AS loc FROM \"/zips\" LIMIT 1",

    "predicate": "containsOnly",

    "expected": [{ "loc" : -72.622739}]
}
