{
  "name": "group by a computed value",

  "data": "zips.data",

  "query": "select substring(city, 0, 1) as \"first\", count(*) as numZips from zips group by substring(city, 0, 1) order by count(*) desc",

  "predicate": "containsAtLeast",
  "expected": [
    { "first": "X", "numZips": 2    },
    { "first": "Z", "numZips": 48   },
    { "first": "C", "numZips": 2692 },
    { "first": "S", "numZips": 2871 }
  ]
}