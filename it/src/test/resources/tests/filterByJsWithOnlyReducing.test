{
  "name": "filter by JS expression, with only reducing projections",

  "data": "zips.data",

  "query": "select max(pop), min(city) from zips where length(city) < 6",

  "predicate": "containsExactly",
  "expected": [
    { "0": 85710, "1": "AARON" }
  ]
}
