{
  "name": "count distinct *",
  "backends": { "mongodb_read_only": "pending" },
  "data": "olympics.data",
  "query": "select count(distinct *) from olympics",
  "predicate": "equalsExactly",
  "expected": [{ "0": 2304 }]
}
